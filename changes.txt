# FINAL PITCH-READY RESET CODE (1 AM EMERGENCY VERSION)
# Run this in a new Colab cell to forcefully refresh your environment.

# 1. Kill everything (Old workers, old redis)
print("Terminating old background processes...")
!pkill -9 -f celery
!pkill -9 -f uvicorn
!pkill -9 -f redis-server

# 2. Pull the latest stability & debug code
print("Syncing latest code from GitHub...")
%cd /content/sensorspine-humaniser-v3
!git pull

# 3. Start fresh
print("Launching clean backend pipeline...")
%cd backend
!chmod +x start.sh
!./start.sh
