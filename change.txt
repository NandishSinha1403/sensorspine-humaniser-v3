# GOOGLE COLAB SETUP CODE (FINAL STABLE VERSION)
# Copy and paste this into your first code cell in Colab.

# 1. Reset directory
%cd /content
!rm -rf sensorspine-humaniser-v3

# 2. Re-install core libraries
!pip install --upgrade amrlib fastapi uvicorn pydantic torch transformers accelerate bitsandbytes python-multipart pyngrok penman

# 3. Clone fresh code
!git clone https://github.com/NandishSinha1403/sensorspine-humaniser-v3.git
%cd sensorspine-humaniser-v3/backend

# 4. MANUAL MODEL DOWNLOAD (100% Stable)
print("Downloading models manually...")
!mkdir -p models

# Download STOG Model (Parsing)
!wget -q --show-progress https://github.com/bjascob/amrlib-models/releases/download/model_stog_bert-v0.1.0/model_stog_bert-v0.1.0.tar.gz
!tar -xzf model_stog_bert-v0.1.0.tar.gz -C models/
!rm model_stog_bert-v0.1.0.tar.gz

# Download GTOS Model (Generation)
!wget -q --show-progress https://github.com/bjascob/amrlib-models/releases/download/model_gtos_t5-v0.1.0/model_gtos_t5-v0.1.0.tar.gz
!tar -xzf model_gtos_t5-v0.1.0.tar.gz -C models/
!rm model_gtos_t5-v0.1.0.tar.gz

print("Setup Complete! Models are ready.")

# --- MAC TERMINAL COMMANDS ---
# Run these on your Mac terminal before running the Colab setup above:
# cd /Users/am_nandish/Documents/sensorspine-humaniser-3
# git add .
# git commit -m "Final backend fixes"
# git push
